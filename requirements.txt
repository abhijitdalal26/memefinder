requests>=2.31.0
beautifulsoup4>=4.12.0
Pillow>=10.0.0
imagehash>=4.3.0
playwright>=1.40.0
numpy>=1.26.0

# --- retrieval / search ---
sentence-transformers>=2.3.0   # bge-large embeddings + bge-reranker
gradio>=4.0.0                  # web UI (inline on Colab via share link)

# --- OCR (Colab) ---
# paddleocr 3.x API (device=, predict()); requires paddlepaddle >= 3.0
paddleocr>=3.0
# On Colab GPU install the GPU build of paddle AFTER paddleocr (CUDA 12.x):
#   !pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/

